#' @title Dynamic Representation of Multiple Sequences
#' @description Characterize multiple sequences at the same time
#' @usage drepMultiple(seqs, type = 's', dim = 2)
#' @param seqs sequences given as a vector of single strings, e.g. c('AGTGGG', 'TTGA') or as a FASTA type
#' @param type 's' - symbolic, 'wb' - grayscale, 'c' - colors
#' @return

drepMultiple <- function(seqs, dim = 2, type = 's'){
  for (seq in seqs){

  }
}
